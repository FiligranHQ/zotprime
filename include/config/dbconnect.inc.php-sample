<?
function Zotero_dbConnectAuth($db) {
	if ($db == 'master') {
		$host = '';
		$port = 3306;
		$db = '';
		$user = '';
		$pass = '';
	}
	
	return array('host'=>$host, 'port'=>$port, 'db'=>$db, 'user'=>$user, 'pass'=>$pass);
}
?>
